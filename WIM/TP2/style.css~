body {
    background-color:#222C28;
}
h1,p {
	font-family: "Courier New", Courier, monospace;
	color: #FFFFFF;
	font-weight: normal;
}
h1 {
	font-size: 2em;
    text-transform: uppercase;
}
h1 b {
    letter-spacing: 2px;
}
.center{
    text-align: center;
}
img.map, map area, img.center {
    margin: auto;
    display: block;
    outline: none;
}
div.overlay {
    position: absolute;
}
div.nav {
    background-color: rgba(255,255,255,0.6);
    top: 40%;
    position: fixed;
    float: right;
}
table, th, td {
    border: 2px solid black;
    border-collapse: collapse;
}
th {
    padding: 2px;
}
td {
    width:60px;
    height:60px;
}
a.inventory {
    cursor: auto;
    color: white;
    font-size: 18px;
    text-decoration: underline;
}
