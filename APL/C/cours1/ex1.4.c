#include <stdio.h>

int main(int argc, char* argv[]) {
  
  int entier;
  scanf("%d", &entier);

  char car;
  scanf(" %c", &car);
  printf("integer : %d\nchar : %c\n", entier, car);

    return 0;
}
